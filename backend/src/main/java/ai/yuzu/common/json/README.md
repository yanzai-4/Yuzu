# ai.yuzu.common.json

> v0.0.5 🍊 JSON helpers.

- `Jsons` — read/write for DB JSON columns and `canonical(...)` (sorted keys) for byte-stable prompt
  rendering, which keeps LLM prompt prefixes cache-friendly.
