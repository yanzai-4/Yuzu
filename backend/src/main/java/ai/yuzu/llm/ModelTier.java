package ai.yuzu.llm;

/**
 * v0.0.7 🍊 The three model tiers configured in the console; every module declares which tier it uses.
 *
 * <p>IMPORTANT = main consciousness; DEFAULT = most modules and tools; LIGHT = monitor summaries.</p>
 */
public enum ModelTier {
    IMPORTANT,
    DEFAULT,
    LIGHT
}
